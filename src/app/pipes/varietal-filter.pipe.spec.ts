import { VarietalFilterPipe } from './varietal-filter.pipe';

describe('VarietalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VarietalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
