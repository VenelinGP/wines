import { SortByVintagePipe } from './sort-by-vintage.pipe';

describe('SortByVintagePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByVintagePipe();
    expect(pipe).toBeTruthy();
  });
});
